import {Image} from 'react-native';

export const LogoVert = (props) => {
    return (
        <Image 
        source = {require('C:/Users/kaiak/Tracker/app/assets/LogoVert.png')}
        style = {props.style}
        />
    );
}